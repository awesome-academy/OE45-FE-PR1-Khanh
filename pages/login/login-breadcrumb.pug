mixin login-breadcrumb
    nav(aria-label="breadcrumb")#breadcrumb
        ol.breadcrumb
            li.breadcrumb-item
                +link() Home 
            li.breadcrumb-item.active(aria-current="page") 
                +link() Đăng Nhập
